# Shared Configuration

Configuration templates and defaults shared across all projects.

## Files

- **defaults.yaml** - Default fleet settings
- **roles.yaml** - Role capability mappings
- **logging.yaml** - Logging configuration
- **paths.yaml** - Standard path conventions

## Project Override

Projects can override via their .fleet/project.yaml