# Project Context Template

Use this template to initialize the `.fleet/` directory for a new project.

## Usage

```bash
cp -r fleet/templates/project_context/ /path/to/project/.fleet/
```

Then edit each file for the specific project.